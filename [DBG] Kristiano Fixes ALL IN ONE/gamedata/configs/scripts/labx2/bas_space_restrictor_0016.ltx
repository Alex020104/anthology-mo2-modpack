[logic]
active = sr_idle

[sr_idle]
; Anthology 2.1 crash fix:
; - removed run_postprocess(duality_circle): the postprocess section does not exist
; - removed bas_no_gravity_anomaly: the xr_effects functor does not exist
; Preserve the valid sound effect and the original transition to the particle section.
on_actor_inside = {+bas_switcher_off} %=play_snd(affects\hit_fist)% sr_particle@zone

[sr_particle@zone]
name = anomaly2\gravity_blast_final
path = bas_sc7_particle
mode = 2
looped = false
